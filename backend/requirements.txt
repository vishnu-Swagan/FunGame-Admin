# Lean runtime requirements for deployment (Render/Docker).
# The original Emergent-generated list is preserved in requirements.emergent.txt
# but cannot install off-platform (emergentintegrations + private litellm wheel).
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
pydantic==2.13.4
email-validator==2.3.0
motor==3.3.1
pymongo==4.6.3
python-dotenv==1.2.2
PyJWT==2.13.0
bcrypt==4.1.3
resend==2.34.0
