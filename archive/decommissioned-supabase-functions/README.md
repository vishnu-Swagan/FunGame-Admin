# Decommissioned Supabase Edge Functions

This tree stores source retained for audit and incident-review purposes. It is
intentionally outside `supabase/functions` and is not part of any deployment
workflow.

Archived functions must not be copied back into the deployable tree or included
in a Supabase CLI deployment. A new production function requires a new security
review, a new name, and an explicit release procedure.
